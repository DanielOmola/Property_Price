from setuptools import setup

setup(
   name='mypackage',
   version='1.0',
   description='A useful module',
   author='Daniel Omola',
   author_email='daniel.omola@foo.com',
   packages=['mypackage']  #same as name
   
)